CREATE SEQUENCE seq_branch_endpoints_id MINVALUE 1 START WITH 1 INCREMENT BY 1;

CREATE TABLE branch_endpoints (
	id BIGINT PRIMARY KEY,
	depot_id BIGINT,
	branch_id BIGINT,
	head_id BIGINT,
	root_id BIGINT
);
