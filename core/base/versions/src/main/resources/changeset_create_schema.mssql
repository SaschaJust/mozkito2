CREATE TABLE changesets (
	id BIGINT,
	depot_id BIGINT,
	commit_hash CHAR(40),
	tree_hash CHAR(40),
	authored_time DATETIME,
	author_id BIGINT,
	commit_time DATETIME,
	committer_id BIGINT,
	subject VARCHAR(MAX),
	body VARCHAR(MAX)
);
