CREATE SEQUENCE seq_tags_id MINVALUE 1 START WITH 1 INCREMENT BY 1;

CREATE TABLE tags (
	id BIGINT PRIMARY KEY,
	depot_id BIGINT,
	changeset_id BIGINT,
	name LONG VARCHAR,
	hash CHAR(40),
	message LONG VARCHAR,
	tagger_id BIGINT,
	tag_time TIMESTAMP 
);
